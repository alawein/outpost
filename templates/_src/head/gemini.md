# Outpost

Gemini CLI reads this guide from `GEMINI.md` at the project root. The prompts install as custom
commands under `.gemini/commands/outpost/`, invoked as `/outpost:<name>`. Read this at the start of
a session. Replace the bracketed text for your project, then delete it.
