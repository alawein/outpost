## Summary

One to three sentences: what this PR does and why.

## What changed

- The key changes, grouped by area if long.

## Testing

What was run and what it showed: `python validate.py`, `python -m pytest -q`, and any manual
check (an install into a scratch project, a smoke run of a prompt).

## Risks and limitations

Known caveats, out-of-scope work, follow-ups. "None" is a valid answer.
Debt taken or closed belongs in docs/DEBT.md in this PR; a decision that meets the bar gets a record.
